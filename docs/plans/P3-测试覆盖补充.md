# P3 补充方案：剩余 9 个模块测试覆盖

## 前置条件

- 项目路径：`C:\Users\秋月\Desktop\WindowStatus`
- 代码规范：Python 3、PyQt5、注释用中文
- 测试运行：`python -m pytest tests/ -v`
- 开工前必读：`plugins/stats/plugin.py`、`kernel/core.py`、`kernel/plugin_manager.py`
- 禁止事项：不改业务代码，只写测试文件

**Goal:** 为剩余 9 个未覆盖模块编写单元测试，覆盖率从 50% 提升到 100%

**测试策略：**
- 纯 Python 逻辑：直接测试
- PyQt5 组件：mock QApplication，只测试逻辑
- Windows API（monitor）：mock ctypes，只测试状态管理
- UI 弹窗（dialog）：不测试（创建 QApplication 会卡住）

---

## CC 注意事项

1. **QApplication 会卡住** — 不要创建真实的 QWidget/QDialog，只测试纯逻辑
2. **mock 策略** — 用 unittest.mock.MagicMock 模拟 kernel、config、event_bus
3. **数据库测试** — 用内存数据库 `:memory:`，测试完自动清理
4. **Windows API** — mock `ctypes.windll`，不调用真实 API
5. **每个测试独立** — setUp 清理状态，tearDown 释放资源
6. **断言要具体** — 不要 `assert result is not None`，要 `assert result == expected`

---

## Task 3.9：kernel/core.py 测试

**Objective:** 覆盖 Kernel 类的核心逻辑

**Files:**
- Create: `tests/test_kernel.py`

**测试函数：**
1. `test_init_creates_event_bus` — 初始化创建 EventBus
2. `test_init_creates_config` — 初始化创建 Config
3. `test_init_creates_plugin_manager` — 初始化创建 PluginManager
4. `test_set_qt_app` — 设置 QApplication 引用
5. `test_start_loads_plugins` — start() 调用 load_plugins
6. `test_stop_unloads_plugins` — stop() 调用 unload_plugins

**Mock 策略：**
```python
from unittest.mock import MagicMock, patch

@pytest.fixture
def mock_kernel():
    """创建 mock Kernel（不启动真实依赖）"""
    with patch('kernel.core.EventBus') as mock_eb, \
         patch('kernel.core.Config') as mock_cfg, \
         patch('kernel.core.PluginManager') as mock_pm:
        from kernel.core import Kernel
        kernel = Kernel('/tmp/config.json', '/tmp/data.db', '/tmp/log.txt')
        yield kernel
```

**完成标准：**
- [ ] 6 个测试全部通过
- [ ] 语法检查：`python3 -m py_compile tests/test_kernel.py`

---

## Task 3.10：kernel/plugin_manager.py 测试

**Objective:** 覆盖 PluginManager 的插件加载/卸载逻辑

**Files:**
- Create: `tests/test_plugin_manager.py`

**测试函数：**
1. `test_discover_plugins` — 发现插件目录下的插件
2. `test_load_plugin` — 加载插件并调用 on_load
3. `test_unload_plugin` — 卸载插件并调用 on_unload
4. `test_enable_plugin` — 启用插件并调用 on_enable
5. `test_disable_plugin` — 禁用插件并调用 on_disable
6. `test_get_plugin` — 按名称获取插件
7. `test_get_all_plugins` — 获取所有插件
8. `test_is_loaded` — 检查插件是否已加载
9. `test_is_enabled` — 检查插件是否已启用
10. `test_sort_plugins_by_dependency` — 拓扑排序

**Mock 策略：**
```python
@pytest.fixture
def mock_plugin_manager():
    """创建 mock PluginManager"""
    mock_kernel = MagicMock()
    mock_kernel.event_bus = MagicMock()
    mock_kernel.config = MagicMock()
    pm = PluginManager(mock_kernel)
    yield pm
```

**完成标准：**
- [ ] 10 个测试全部通过
- [ ] 语法检查：`python3 -m py_compile tests/test_plugin_manager.py`

---

## Task 3.11：plugins/monitor/plugin.py 测试

**Objective:** 覆盖 Monitor 插件的状态管理逻辑

**Files:**
- Create: `tests/test_monitor.py`

**测试函数：**
1. `test_window_info_equality` — WindowInfo 相等性判断
2. `test_window_info_hash` — WindowInfo 哈希
3. `test_window_info_str` — WindowInfo 字符串表示
4. `test_get_idle_seconds` — 空闲秒数获取（mock ctypes）
5. `test_monitor_init` — Monitor 插件初始化
6. `test_on_category_matched` — 分类匹配事件处理
7. `test_on_idle_detected` — 空闲检测事件处理
8. `test_on_idle_resumed` — 空闲恢复事件处理
9. `test_get_last_window` — 获取上一个窗口
10. `test_is_idle` — 检查是否空闲

**Mock 策略：**
```python
@pytest.fixture
def mock_monitor():
    """创建 mock Monitor 插件"""
    mock_kernel = MagicMock()
    mock_kernel.event_bus = MagicMock()
    mock_kernel.config = MagicMock()
    mock_kernel.config.get.return_value = True  # idle_detection enabled
    monitor = MonitorPlugin(mock_kernel)
    yield monitor
```

**完成标准：**
- [ ] 10 个测试全部通过
- [ ] 语法检查：`python3 -m py_compile tests/test_monitor.py`

---

## Task 3.12：plugins/stats/plugin.py 测试

**Objective:** 覆盖 Stats 插件的统计逻辑

**Files:**
- Create: `tests/test_stats_plugin.py`

**测试函数：**
1. `test_connect` — 数据库连接
2. `test_create_tables` — 创建表结构
3. `test_get_week_start` — 获取周一日期
4. `test_log_activity` — 记录活动
5. `test_record_current_activity` — 记录当前活动
6. `test_get_today_stats` — 获取今日统计
7. `test_get_week_stats` — 获取本周统计（实时聚合）
8. `test_get_month_stats` — 获取本月统计
9. `test_get_yesterday_stats` — 获取昨日统计
10. `test_aggregate_daily` — 聚合每日数据
11. `test_aggregate_weekly` — 聚合每周数据
12. `test_aggregate_monthly` — 聚合每月数据

**Mock 策略：**
```python
@pytest.fixture
def stats_plugin(tmp_path):
    """创建 Stats 插件（使用临时数据库）"""
    mock_kernel = MagicMock()
    mock_kernel.event_bus = MagicMock()
    mock_kernel.config = MagicMock()
    mock_kernel.db_path = str(tmp_path / "test.db")
    mock_kernel.logger = MagicMock()
    plugin = StatsPlugin(mock_kernel)
    plugin.on_load()  # 初始化数据库
    yield plugin
    plugin.close()
```

**完成标准：**
- [ ] 12 个测试全部通过
- [ ] 语法检查：`python3 -m py_compile tests/test_stats_plugin.py`

---

## Task 3.13：plugins/settings/plugin.py 测试

**Objective:** 覆盖 Settings 插件的设置保存逻辑

**Files:**
- Create: `tests/test_settings_plugin.py`

**测试函数：**
1. `test_get_plugins_info` — 获取插件信息列表
2. `test_on_save_categories` — 保存分类规则
3. `test_on_save_plugins` — 保存插件配置
4. `test_on_save_theme` — 保存主题配置
5. `test_on_save_autostart` — 保存开机自启配置
6. `test_on_save_minimize_to_tray` — 保存最小化到托盘配置
7. `test_on_save_topmost` — 保存窗口置顶配置
8. `test_set_autostart_enabled` — 启用开机自启（mock winreg）
9. `test_set_autostart_disabled` — 禁用开机自启（mock winreg）

**Mock 策略：**
```python
@pytest.fixture
def settings_plugin():
    """创建 Settings 插件"""
    mock_kernel = MagicMock()
    mock_kernel.event_bus = MagicMock()
    mock_kernel.config = MagicMock()
    mock_kernel.config.get_categories.return_value = {}
    mock_kernel.config.get.return_value = {"monitor": True}
    plugin = SettingsPlugin(mock_kernel)
    yield plugin
```

**完成标准：**
- [ ] 9 个测试全部通过
- [ ] 语法检查：`python3 -m py_compile tests/test_settings_plugin.py`

---

## Task 3.14：plugins/tray/plugin.py 测试

**Objective:** 覆盖 Tray 插件的菜单创建逻辑

**Files:**
- Create: `tests/test_tray.py`

**测试函数：**
1. `test_tray_init` — Tray 插件初始化
2. `test_create_menu` — 创建菜单（mock QMenu）
3. `test_toggle_top` — 切换置顶状态
4. `test_set_opacity` — 设置透明度
5. `test_on_category_matched` — 分类匹配事件处理（更新图标）
6. `test_update_top_state` — 更新置顶状态显示

**Mock 策略：**
```python
@pytest.fixture
def tray_plugin():
    """创建 Tray 插件（mock QSystemTrayIcon）"""
    mock_kernel = MagicMock()
    mock_kernel.event_bus = MagicMock()
    mock_kernel.config = MagicMock()
    plugin = TrayPlugin(mock_kernel)
    yield plugin
```

**完成标准：**
- [ ] 6 个测试全部通过
- [ ] 语法检查：`python3 -m py_compile tests/test_tray.py`

---

## Task 3.15：plugins/about/plugin.py 测试

**Objective:** 覆盖 About 插件的事件处理

**Files:**
- Create: `tests/test_about.py`

**测试函数：**
1. `test_about_init` — About 插件初始化
2. `test_on_show_about` — 显示关于弹窗事件处理

**Mock 策略：**
```python
@pytest.fixture
def about_plugin():
    """创建 About 插件"""
    mock_kernel = MagicMock()
    mock_kernel.event_bus = MagicMock()
    plugin = AboutPlugin(mock_kernel)
    yield plugin
```

**完成标准：**
- [ ] 2 个测试全部通过
- [ ] 语法检查：`python3 -m py_compile tests/test_about.py`

---

## Task 3.16：plugins/base.py 测试

**Objective:** 覆盖 Plugin 基类的方法

**Files:**
- Create: `tests/test_base.py`

**测试函数：**
1. `test_plugin_init` — Plugin 基类初始化
2. `test_get_plugin` — 获取其他插件
3. `test_get_all_plugins` — 获取所有插件
4. `test_main_window` — 获取主窗口引用
5. `test_on_load` — 默认 on_load 实现
6. `test_on_unload` — 默认 on_unload 实现
7. `test_on_enable` — 默认 on_enable 实现
8. `test_on_disable` — 默认 on_disable 实现
9. `test_str` — 字符串表示
10. `test_repr` — repr 表示

**Mock 策略：**
```python
@pytest.fixture
def base_plugin():
    """创建 Plugin 基类实例"""
    mock_kernel = MagicMock()
    plugin = Plugin(mock_kernel)
    yield plugin
```

**完成标准：**
- [ ] 10 个测试全部通过
- [ ] 语法检查：`python3 -m py_compile tests/test_base.py`

---

## 验收标准

1. `python3 harness/checks.py` 约束检查 0 违规
2. `python -m pytest tests/ -v` 所有测试通过（允许预先存在的 2 个 failed）
3. 测试覆盖率：18/18 模块 = 100%

---

*编写时间：2026-06-28*
*任务数：8*
*预计测试用例：65 个*

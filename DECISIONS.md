# WindowStatus 决策档案

> 只增不改，记录所有重要技术决策
> 最后更新：2026-06-07-22:00

---

## D-001：选择 PyQt5 而非 Electron/Tkinter
- **日期：** 2026-05-27
- **背景：** 桌面窗口状态显示器需要常驻后台，技术选型
- **决策：** PyQt5
- **理由：** 一个 Python 进程，内存几十 MB，比 Electron 轻十倍。Tkinter 的 UI 太丑，半透明圆角窗口做不出来。常驻后台工具，资源占用是硬约束
- **代价：** PyQt5 的 API 不直觉，文档散，坑多

## D-002：插件化架构
- **日期：** 2026-05-28
- **背景：** v1.0.0 单文件 1020 行，改功能要翻半天
- **决策：** 拆分为 core/ + plugins/，Plugin 基类 + EventBus + Config
- **理由：** 功能隔离、独立开关、便于回滚、便于并行开发。插件间必须通过 EventBus 通信，不能直接调用
- **代价：** 跨插件通信比直接调用多一层抽象

## D-003：纯代码绘制气泡（不用外部素材）
- **日期：** 2026-05-29
- **背景：** 悬浮气泡 UI 实现方案选择
- **决策：** QPainter 纯代码绘制
- **理由：** 版权顾虑，不用外部素材。大小/颜色/圆角全部参数化，调整方便
- **代价：** 调试成本比图片素材高（分隔线反复调了 7 次）

## D-004：放弃尾巴改用小气泡
- **日期：** 2026-05-30
- **背景：** 气泡和桌宠之间的连接方式
- **决策：** 用小圆点（drawEllipse, r=3px）代替三角形尾巴
- **理由：** QPainterPath.united()/addPath() 合并路径有接缝（浮点精度问题），小圆点简单可靠
- **教训：** 遇到图形渲染的底层问题，换方案比修方案快

## D-005：删除气泡拖拽功能
- **日期：** 2026-05-30
- **背景：** 气泡拖拽 + 桌宠拖拽的交互太复杂
- **决策：** 只保留桌宠拖拽，删除气泡拖拽
- **理由：** 4 种拖拽状态 × 3 种保护操作 = 12 种组合，状态机复杂度爆炸。删掉气泡拖拽后代码减少 150 行，复杂度大幅降低
- **教训：** 功能不是越多越好。当交互复杂度超过承受能力时，砍掉一个比修到完美更划算

## D-006：一个 UI 元素只由一个插件负责
- **日期：** 2026-05-29
- **背景：** v3.3.0 双气泡 bug（桌宠插件和 overlay 插件都创建了 widget）
- **决策：** overlay 负责气泡绘制，桌宠只负责图片显示
- **理由：** 两个插件同时创建 widget 会冲突
- **教训：** 重构时要彻底删除旧代码，不注释、不留"备用"

## D-007：Config 线程安全
- **日期：** 2026-05-30
- **背景：** 多线程下配置读写竞态条件
- **决策：** 所有公开方法加 threading.RLock()
- **理由：** 之前的简单 dict 在多线程下会出竞态条件

## D-008：进程内重载替代 os.execv
- **日期：** 2026-05-30
- **背景：** PyInstaller 打包后 os.execv 和 subprocess 都无法正确重启
- **决策：** 改为进程内重载（Events.RESTART → stop → 重建 EventBus/Config/PluginManager → start）
- **理由：** PyInstaller 嵌入式解释器不支持 execv

## D-009：插件解耦（事件驱动）
- **日期：** 2026-05-30
- **背景：** settings 插件直接调用 rules/overlay 的方法
- **决策：** 改为事件驱动（RULES_RELOAD、OVERLAY_SET_THEME）
- **理由：** 降低插件间耦合度，便于独立开发和测试

## D-010：桌宠长按 200ms 触发拖拽
- **日期：** 2026-05-30
- **背景：** 点击也触发拖拽，体验不好
- **决策：** 长按 200ms 才触发拖拽，短按为普通点击
- **理由：** 300ms 太迟钝，200ms 手感更流畅

## D-011：统计界面 Apple Screen Time 风格
- **日期：** 2026-05-31
- **背景：** 统计界面重做
- **决策：** SummaryCard + DonutChart + CategoryRow，QGridLayout 布局
- **理由：** 副标题和主数据要分别放在独立的布局格子里，QGridLayout 天然支持每个格子独立居中
- **教训：** addStretch() 会把内容推到底部，不要在需要居中的场景使用

## D-012：桌宠位置记忆（TDD 方式）
- **日期：** 2026-05-31
- **背景：** 之前两次实现失败（_delayed_init 覆盖问题），主人明确放弃
- **决策：** 用 TDD 方式重新实现，先写 7 个测试再写代码
- **理由：** 先写测试定义清楚"哪些路径不允许覆盖"，再逐个实现。比"写一堆代码→发现覆盖→回滚"靠谱
- **关键：** 4 个覆盖路径全部加 `_user_positioned` 检查

## D-013：删除 Reminders 插件
- **日期：** 2026-06-03
- **背景：** QMessageBox 弹窗有 bug，实际体验不好
- **决策：** 直接删除而不是修复
- **理由：** 功能形同虚设，主人决定删除

## D-014：桌宠自定义素材方案
- **日期：** 2026-05-31
- **背景：** 桌宠支持自定义素材，无需重新打包
- **决策：** exe 同目录优先读取，读不到时回退到内置素材（sys._MEIPASS）
- **理由：** 最初实现动态加载功能，因复杂度高回滚，改为简单方案
- **教训：** 简单方案比花哨方案更可靠

---

*基于 devlog（2026-05-27 ~ 2026-06-07）整理*

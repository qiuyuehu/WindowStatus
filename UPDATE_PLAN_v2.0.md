# WindowStatus v2.0 更新方案

## 背景

当前 v1.0 是单文件架构，所有功能耦合在一起，存在以下问题：
- 代码耦合度高，难以维护和扩展
- PyQt5 是重型依赖，即使用不到也会打包进去
- 没有配置文件，参数硬编码
- 没有日志系统，调试困难
- 无法按需启用/禁用功能

参考优化建议，采用**插件化架构**重构。

---

## 方案一：轻量重构（推荐）

**思路：** 保留当前功能，重构为插件架构，最小改动

**改动范围：**
- 拆分代码为多个模块
- 引入配置文件
- 添加基础日志
- 优化异常处理

**文件结构：**
```
WindowStatus/
├── core/
│   ├── __init__.py
│   ├── monitor.py       # 窗口监控（内核）
│   ├── classifier.py    # 分类引擎（内核）
│   ├── config.py        # 配置管理
│   └── logger.py        # 日志模块
├── plugins/
│   ├── __init__.py
│   ├── overlay.py       # 悬浮窗插件
│   ├── tray.py          # 系统托盘插件
│   ├── stats.py         # 统计插件
│   └── autostart.py     # 开机自启动插件
├── main.py              # 程序入口
├── config.json          # 配置文件
├── build.py             # 打包脚本
└── README.md
```

**执行步骤：**
1. 创建项目结构
2. 拆分核心模块（monitor、classifier）
3. 拆分插件模块（overlay、tray、stats）
4. 添加配置管理
5. 添加日志模块
6. 优化异常处理
7. 更新打包脚本

**优点：**
- 改动最小，风险低
- 保持现有功能
- 代码结构清晰

**缺点：**
- 没有真正的插件热加载
- 仍然是单进程架构

---

## 方案二：完整插件化

**思路：** 完全重写，实现真正的插件系统

**改动范围：**
- 实现插件加载器
- 每个功能独立为插件
- 支持插件热加载/卸载
- 实现事件总线

**文件结构：**
```
WindowStatus/
├── kernel/
│   ├── __init__.py
│   ├── core.py          # 核心引擎
│   ├── event_bus.py     # 事件总线
│   ├── plugin_loader.py # 插件加载器
│   └── config.py        # 配置管理
├── plugins/
│   ├── monitor/
│   │   ├── __init__.py
│   │   └── plugin.py    # 窗口监控插件
│   ├── overlay/
│   │   ├── __init__.py
│   │   └── plugin.py    # 悬浮窗插件
│   ├── tray/
│   │   ├── __init__.py
│   │   └── plugin.py    # 系统托盘插件
│   ├── stats/
│   │   ├── __init__.py
│   │   └── plugin.py    # 统计插件
│   └── rules/
│       ├── __init__.py
│       └── plugin.py    # 规则触发插件
├── main.py
├── config.json
└── README.md
```

**执行步骤：**
1. 设计插件接口规范
2. 实现事件总线
3. 实现插件加载器
4. 重写核心引擎
5. 逐个实现插件
6. 测试插件热加载
7. 更新打包脚本

**优点：**
- 真正的插件化，可扩展性强
- 功能完全解耦
- 支持热加载/卸载

**缺点：**
- 改动大，风险高
- 开发周期长
- 架构复杂度高

---

## 方案三：渐进式优化

**思路：** 不重构架构，逐步优化现有代码

**改动范围：**
- 优化异常处理
- 添加配置文件
- 优化性能（动态刷新、事件去重）
- 添加日志

**执行步骤：**
1. 添加配置文件支持
2. 优化异常处理（捕获窗口句柄异常）
3. 添加日志模块
4. 优化性能（动态刷新频率）
5. 优化打包脚本（减小体积）

**优点：**
- 改动最小，最安全
- 保持现有代码结构
- 快速完成

**缺点：**
- 没有架构改进
- 后续扩展困难
- 代码仍然耦合

---

## 对比

| 维度 | 方案一 | 方案二 | 方案三 |
|------|--------|--------|--------|
| 改动大小 | 中等 | 大 | 小 |
| 风险 | 低 | 高 | 最低 |
| 开发周期 | 2-3天 | 5-7天 | 1天 |
| 架构改进 | 有 | 完整 | 无 |
| 可扩展性 | 好 | 最好 | 差 |
| 维护性 | 好 | 最好 | 差 |

---

## 我的推荐

**方案一（轻量重构）**

理由：
1. 当前代码量不大，重构风险可控
2. 插件化架构为后续扩展打下基础
3. 开发周期适中，2-3天可完成
4. 保持现有功能，不会丢失

---

*方案由衾衾整理，等主人确认后开工。*
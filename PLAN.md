# WindowStatus 执行计划

## 项目概述

Windows 窗口状态显示器，参考 Discord/Steam 设计，实时显示当前活动窗口的分类状态。

## 功能清单

| 功能 | 状态 | 说明 |
|------|------|------|
| 窗口监控 | ✅ 已完成 | 事件驱动，切换窗口时更新 |
| 自动分类 | ✅ 已完成 | 游戏/办公/摸鱼/开发/其他 |
| 悬浮窗显示 | ✅ 已完成 | 深色主题、圆角、半透明 |
| 可拖拽 | ✅ 已完成 | 左键拖拽移动位置 |
| 透明度设置 | ✅ 已完成 | 右键菜单调整 50%~100% |
| 置顶/取消置顶 | ✅ 已完成 | 右键菜单切换 |
| 自定义分类 | ✅ 已完成 | 设置界面添加/编辑/删除 |
| 智能识别 | ✅ 已完成 | 根据窗口标题关键词自动判断 |
| 适配用户软件 | ⏳ 待完成 | 需要用户软件列表 |
| 系统托盘 | ✅ 已完成 | 托盘图标 + 右键菜单 |
| 统计窗口 | ❌ 未开始 | 今日时间线、分类饼图 |
| 数据存储 | ❌ 未开始 | SQLite 记录历史 |
| 开机自启动 | ❌ 未开始 | 注册表写入 |
| 打包发布 | ❌ 未开始 | PyInstaller 打包 |

## 执行步骤

### 第一阶段：适配用户软件（当前）

**步骤 1：收集用户软件信息**
- 运行 `scan_software.py` 扫描已安装软件
- 用户把结果发给我
- 分析软件列表，适配分类规则

**步骤 2：更新分类规则**
- 根据用户软件更新默认分类
- 添加用户常用软件的规则
- 测试识别效果

### 第二阶段：功能完善

**步骤 3：完善设置界面**
- 分类规则可视化编辑
- 规则测试功能（输入窗口标题，预览分类结果）
- 导入/导出配置

**步骤 4：统计功能**
- 记录每个窗口的使用时长
- 今日使用时间线
- 分类使用时长统计
- 数据存储（SQLite）

### 第三阶段：优化和发布

**步骤 5：界面美化**
- 动画效果（分类切换时的渐变）
- 更多主题选项
- 图标设计

**步骤 6：打包发布**
- PyInstaller 打包成 exe
- 编写 README
- 发布到 GitHub

## 技术细节

### 窗口监控（已完成）

使用 `SetWinEventHook` 监听 `EVENT_SYSTEM_FOREGROUND` 事件，CPU 占用极低。

### 分类规则（已完成）

支持两种匹配方式：
1. **进程名匹配** — 精确匹配进程名（如 `chrome.exe`）
2. **窗口标题匹配** — 通配符匹配（如 `*B站*`）

### 智能识别（已完成）

根据窗口标题中的关键词自动判断分类：
- 游戏：steam, game, 原神, 英雄联盟...
- 办公：office, 文档, 钉钉, 飞书...
- 摸鱼：视频, b站, 微博, 抖音...
- 开发：code, vscode, pycharm, terminal...

### 透明度设置（已完成）

右键托盘菜单 → 透明度 → 选择百分比（50%~100%）

### 置顶/取消置顶（已完成）

右键托盘菜单 → 置顶/取消置顶

---

*计划由衾衾整理，等主人确认后执行。*